import { DigitcountPipePipe } from './digitcount-pipe.pipe';

describe('DigitcountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DigitcountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
